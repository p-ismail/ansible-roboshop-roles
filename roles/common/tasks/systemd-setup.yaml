  - name: copy the {{ component }} service file
    ansible.builtin.template:
      src: "{{ component }}.service.j2" #here j2 is jinja 2
      dest: " /etc/systemd/system/{{ component }}.service "
    notify: restart "{{ component }}"

  - name: daemon reload
    ansible.builtin.systemd_service:
      daemon_reload: true